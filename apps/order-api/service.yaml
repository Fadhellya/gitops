apiVersion: v1
kind: Service
metadata:
  name: order-api
  namespace: vti-shop-apps
  labels:
    app: order-api
spec:
  selector:
    app: order-api
  ports:
    - name: http
      port: 8080
      targetPort: 8080
